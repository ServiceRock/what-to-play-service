if [ ${ENVIRONMENT} ]; then echo "started ember build --environment=${ENVIRONMENT}" && ember build --environment=${ENVIRONMENT}; else echo "started ember build --environment=production" && ember build -prod; fi